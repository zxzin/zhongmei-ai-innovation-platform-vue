# Vue 版本协作计划

## 唯一开发入口

后续产品设计与前端开发统一在本仓库进行。旧单文件 Demo 只作为视觉与需求核对资料，不再直接修改，也不复制旧页面脚本回 Vue 工程。

当前版本是高保真前端展示，尚未接入真实后端。页面中的业务记录、运行状态和统计结果均来自 `src/data/` 的演示台账；对客户展示时不得把模拟数据表述为已接入的生产数据。

## 两人协作分区

为减少同时修改同一个文件造成的冲突，默认按下面的范围分工。

### 平台公共层

由当前负责整体整合的人维护，改动前需要先在沟通中说明：

- `src/layouts/`：顶部栏、侧边栏、账号入口和聚焦工作区。
- `src/router/`：页面路由、角色访问限制和旧地址兼容。
- `src/stores/`：身份、界面偏好、任务和创新性分析共享状态。
- `src/styles/index.css`：全局设计变量、公共组件和响应式规则。
- `src/data/platform.js`：平台级名称、导航和角色配置。

### 业务页面层

可以按页面独立领取，完成后交给整体负责人复验：

- 六项智能应用：`src/pages/apps/`、`QaAgentPage.vue`、`InnovationPage.vue`。
- 工作台与历史：`AgentHubPage.vue`、`HistoryPage.vue`。
- 驾驶舱：`CockpitPage.vue`、`src/components/LineChart.vue`、`DonutChart.vue`。
- 管理与个人：`AdminPage.vue`、`ProfilePage.vue`、`KnowledgeBasePage.vue`。

### 共享业务配置

下面的文件会影响多个页面，修改前必须先同步最新版本，并说明修改目的：

- `src/data/agents.js`：六项智能应用的名称、说明、流程和输入提示。
- `src/data/demo.js`：客户展示用演示台账、来源和统计口径。
- `src/components/`：跨页面复用组件。

## 每次任务的协作流程

1. 开始前同步远端：

   ```bash
   git pull origin main
   ```

2. 在沟通中说明本次负责的页面或文件，避免两人同时修改共享文件。
3. 只修改本次任务涉及的文件；发现需要改共享文件时，先同步确认。
4. 完成后执行：

   ```bash
   npm install
   npm run build
   ```

5. 在浏览器中检查桌面、普通窗口和窄窗口，至少覆盖 1366px、1024px 和 768px 宽度。
6. 只暂存本次文件：

   ```bash
   git add -- <本次修改的文件>
   git diff --cached --check
   git commit -m "vue: 简要说明本次完成内容"
   git push origin main
   ```

7. 推送后说明：修改内容、涉及文件、验证结果和待确认事项。

## 合并前检查清单

- 页面结构符合已确认的产品功能架构，六项智能应用及双角色创新性分析没有缺项。
- 管理中心、个人中心和用户权限职责分离；普通用户不可进入管理员页面。
- 驾驶舱指标有业务来源、统计主键和计算口径，不使用无法解释的完成度或进度数据。
- 所有按钮都能点击，点击后的页面、抽屉、弹窗或状态变化符合前后条件。
- 页面中没有“模拟接口”“内部说明”“实现边界”等面向研发的提示语。
- 1366px、1024px、768px 下无横向溢出、文字遮挡、按钮错位和图表裁切。
- 浏览器控制台无错误，`npm run build` 通过。

## 冲突处理

- 推送被拒绝时先执行 `git pull origin main`，确认差异后再处理，不强制覆盖远端。
- 同一文件出现冲突时保留双方有效改动，不能直接选择整份“本地版本”或“远端版本”。
- 未经确认不要修改旧 Demo、会议材料、XMind、历史版本和需求归档。
- 不使用 `git add .` 或 `git add -A`，避免把本机过程文件和其他人的工作一并提交。
